
from utils import utils

class JoinGameRequest0Serializer:
    data_dict = [
        {'name': 'mediusid', 'n_bytes': 2, 'cast': None}
    ]

class JoinGameRequest0Handler:
    def process(self, serialized, monolith, con):
        raise Exception('Unimplemented Handler: JoinGameRequest0Handler')

