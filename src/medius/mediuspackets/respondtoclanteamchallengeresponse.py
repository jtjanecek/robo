
from utils import utils

class RespondToClanTeamChallengeResponseSerializer:
    data_dict = [
        {'name': 'mediusid', 'n_bytes': 2, 'cast': None}
    ]

class RespondToClanTeamChallengeResponseHandler:
    def process(self, serialized, monolith, con):
        raise Exception('Unimplemented Handler: RespondToClanTeamChallengeResponseHandler')

