
from utils import utils

class GetLadderStatsWide_wIDArraySerializer:
    data_dict = [
        {'name': 'mediusid', 'n_bytes': 2, 'cast': None}
    ]

class GetLadderStatsWide_wIDArrayHandler:
    def process(self, serialized, monolith, con):
        raise Exception('Unimplemented Handler: GetLadderStatsWide_wIDArrayHandler')

