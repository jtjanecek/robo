
from utils import utils

class UtilUpdateLobbyWorldResponseSerializer:
    data_dict = [
        {'name': 'mediusid', 'n_bytes': 2, 'cast': None}
    ]

class UtilUpdateLobbyWorldResponseHandler:
    def process(self, serialized, monolith, con):
        raise Exception('Unimplemented Handler: UtilUpdateLobbyWorldResponseHandler')

