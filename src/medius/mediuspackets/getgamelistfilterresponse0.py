
from utils import utils

class GetGameListFilterResponse0Serializer:
	data_dict = [
		{'name': 'mediusid', 'n_bytes': 2, 'cast': None}
	]

class GetGameListFilterResponse0Handler:
	def process(self, serialized, monolith, con):
		raise Exception('Unimplemented Handler: GetGameListFilterResponse0Handler')

